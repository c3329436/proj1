#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void CaesarCipher(char* string, int key); //the function is defined here to be used later in the main code

  int main(void)
  {
    int key = 1
    char string[100]; //For this cipher the variables are the key nad the string, thiey are established here
    
    printf("\nPlease enter a string:\t");  //Asks th euser to enter the text to be encrypted/decrypted
    gets(string, sizeof(string), stdin);  //gets the inputed text and stores it in the string variable
    printf("\nThe ciphered string is:")
  }
  void CaesarCipher(char* string, int key);
  {
      int i = 0;
      char cipher;
      while(string[i] != '\0' && strlen(string) - 1 > i ){  //while i does not equal zero
      cipher =                      
          
          
      }          
      
  }

    
  return 0;
  }
